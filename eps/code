//Access I2C from python using smbus library:

Import smbus;		//Import smbus library

DEVICE_ADDR  = #;	//Address of EPS
bus = smbus.SMBus(1);	//Create object
bus.write_byte_data(DEVICE_ADDR, 0x00, 0x01);


//Check for connected devices:

Sudo i2cdetect -y 1; // y 1 for I2C1 port

//Reading: 
//Expected states of all PDMS (0x43):

b = bus.read_byte_data(EPS_ADDR, 0x43);
print(b);

//Get initial state of all PDMs (0x44):

b = bus.read_byte_data(EPS_ADDR, 0x44);
print(b);

//Get actual status of specific PDM (see if tripped or not): 0x54 

b = bus.read_byte_data(EPS_ADDR, 0x54);
print(b);

//Writing:
//Set all PDMs to initial state: 0x45

bus.write_byte_data(EPS_ADDR, 0x54, N);
